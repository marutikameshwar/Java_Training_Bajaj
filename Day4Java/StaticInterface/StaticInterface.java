package Day4Java.StaticInterface;

interface StaticInterface
{
    static void division(int a,int b)
    {
        System.out.println("inside the static method which performs division: "+a/b);
    }
}